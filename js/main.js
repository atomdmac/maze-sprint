require.config({
    baseUrl: "js"
});
define(
// Requirements
["map", "mini-map", "first-person", "lib/jquery", "lib/undo-manager"],

// Module definition
function (Map, MiniMap, FirstPerson, $, UndoManager) {
    // TODO
});